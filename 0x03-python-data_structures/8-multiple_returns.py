#!/usr/bin/python3


def multiple_returns(sentence):
    if sentence is not None:
        if x > 0:
            x = len(sentence)
            return (x, sentence[0])
        else:
            return (0, 'None')
