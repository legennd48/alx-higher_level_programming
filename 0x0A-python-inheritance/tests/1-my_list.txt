======================
Module: 1. My list
======================
Module test for module stated above
==============
SUCCESS CASES:
==============

#First impor the module in questuion

    >>> MyList = __import__('1-my_list').MyList

#Standard usage

    >>> my_list = MyList()
    >>> type(my_list)
    <class '1-my_list.MyList'>

::

    >>> print(my_list)
    []

::

    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> print(my_list)
    [1, 4, 2, 3, 5]

::

    >>> my_list = MyList([1, 2, 3])
    >>> print(my_list)
    [1, 2, 3]

::
modifying list

    >>> my_list[1] = 6
    >>> print(my_list)
    [1, 6, 2, 3, 5]

#test removing an element using remove method

::

    >>> my_list.remove(6)
    >>> print(my_list)
    [1, 2, 3, 5]


::

    >>> my_list = MyList()
    >>> print(my_list.print_sorted)
    <bound method MyList.print_sorted of []>

::

    >>> my_list.print_sorted
    <bound method MyList.print_sorted of [1, 4, 2, 3, 5]>

::

    >>> my_list = MyList()
    >>> my_list.append("Edward")
    >>> my_list.append("Ortiz")
    >>> my_list.append("Jhon")
    >>> my_list.append("Doe")
    >>> my_list.append("Mr Puffles")
    >>> print(my_list)
    ['Edward', 'Ortiz', 'Jhon', 'Doe', 'Mr Puffles']

::

    >>> my_list.print_sorted()
    ['JaneDoe', 'Eys', 'grand', 'steps', 'Grand']

==========
FAIL CASES
==========


::

    >>> my_list.print_sorted(1)
    Traceback (most recent call last):
    TypeError: print_sorted() takes 1 positional argument but 2 were given


::

    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []

::

    >>> my_list = MyList([3, "super", "Gene", 2])
    >>> my_list.print_sorted()
    Traceback (most recent call last):
    TypeError: unorderable types: str() < int()
